description: >
  Install Hetzner Cloud CLI (hcloud) for example.
usage:
  version: 2.1
  orbs:
    hcloud-cli: irajtaghlidi/hcloud-cli@x.y.z # change version number
  workflows:
    use-my-orb:
      jobs:
        - hcloud-cli/install
